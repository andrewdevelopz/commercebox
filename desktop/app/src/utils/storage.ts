/**
 *  This module will handle all actions to persist data to the hard drive.
 *  This module is based on storing data inside of `LocalData` in windows
 *  and it's respected parts depending on the OS (linux app data is in bin).
 */
